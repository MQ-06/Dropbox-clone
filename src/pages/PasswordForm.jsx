//Password Page (PasswordForm):

// Form for users whose email already exists.
// Takes the password and sends it to the backend for authentication.

import React from 'react'

const PasswordForm = () => {
  return (
    <div>PasswordForm</div>
  )
}

export default PasswordForm